# Clean Laravel 12 + Tailwind Starter

This starter is prepared for Antigravity/Git usage.

## Setup

```bash
composer create-project laravel/laravel scenario2
cd scenario2

npm install
npm install -D tailwindcss @tailwindcss/vite
```

Copy these starter files into your Laravel project.

## Run

```bash
php artisan serve
npm run dev
```